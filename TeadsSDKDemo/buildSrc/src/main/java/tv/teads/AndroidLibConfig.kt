package tv.teads

/**
 * Android SDK configuration constants.
 * These values are used across all Android modules in the project.
 */
object AndroidLibConfig {
    const val compileSdk = Versions.compileSdk
    const val minSdk = Versions.minSdk
    const val targetSdk = Versions.targetSdk
}