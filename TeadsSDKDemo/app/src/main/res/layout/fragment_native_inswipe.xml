<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <tv.teads.teadssdkdemo.utils.external.PagerSlidingTabStrip
        android:id="@+id/sliding_tabs"
        android:layout_width="match_parent"
        android:layout_height="@dimen/tab_height"
        android:background="?attr/colorPrimary"
        android:textSize="@dimen/tab_sliding_textsize"
        app:pstsIndicatorHeight="@dimen/tab_sliding_height"
        app:pstsTextStyle="normal"
        app:pstsIndicatorColor="@color/accent"/>

    <tv.teads.sdk.publisher.TeadsLockableViewPager
        android:id="@+id/viewpager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white"/>

</LinearLayout>